Atoms

This directory contains basic, single-purpose UI elements.

When creating an atom:
- Start the file with: "This component is an Atom because..."
- Use semantic HTML and accessibility attributes.
- Use BEM class names: `block__element--modifier`.
- No inline `style` props; use CSS variables from the global stylesheet.
- Keep atoms tiny and reusable.
